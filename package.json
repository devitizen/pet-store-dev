{
  "name": "backend",
  "version": "1.0.0",
  "description": "backend for lab 7",
  "main": "index.js",
  "scripts": {
    "prebuild": "cd client && npm install && npm run build",
    "start": "npm install && node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "sqlite3": "^5.0.2"
  }
}
